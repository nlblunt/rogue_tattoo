Edit Admins

<table>
<th>Name</th>
<% @admins.each do |admin| %>
	<tr>
		<td><%= admin.email %></td>
		<td><%= link_to "Delete User", delete_admin_path(:id => admin.id), method: :delete %>
	</tr>
<% end %>